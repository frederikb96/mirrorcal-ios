apiVersion: v1
kind: Service
metadata:
  name: {{ include "mirrorcal-push.fullname" . }}
  labels:
    {{- include "mirrorcal-push.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "mirrorcal-push.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
